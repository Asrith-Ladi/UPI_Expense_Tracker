# Optional: Google OAuth Web client ID for Gmail integration (Email tab).
# Create at https://console.cloud.google.com/ — enable Gmail API, add authorized JavaScript origin (e.g. http://localhost:5173)
VITE_GOOGLE_CLIENT_ID=
